name: Secret Scan (gitleaks)

on:
  push:
  pull_request:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # needed for PR diff scans

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2.2.0
        with:
          args: detect --no-git --redact --config=.gitleaks.toml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}name: Secret Scan (gitleaks)

on:
  push:
  pull_request:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
<<<<<<< HEAD
        with:
          fetch-depth: 0   # <-- important for PR scan mode

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2.2.0
        with:
          args: detect --no-git --redact --config=.gitleaks.toml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
=======
      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        with:
          config: .gitleaks.toml
          redact: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: detect --no-git -v --config=.gitleaks.toml --redact
>>>>>>> 22c29db (chore: add requirements.txt for environment setup)
