# Core

Core holds threat model primitives and authority-channel definitions that the
harness and validators rely on.

- Threat model: see `docs/threat_model.md`.
- Determinism: `aixt/core/env.py`.
- Trace artifacts: `aixt/core/trace.py`.
