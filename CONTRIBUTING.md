# Contributing

Thanks for your interest! To contribute safely:
- Do not include real API keys, PII, or exploit strings in PRs.
- Keep artifacts **sanitized**. If you need to share sensitive reproductions, open an issue and we’ll coordinate privately.
- Run `pytest` (if present) and `pre-commit` locally before pushing.
